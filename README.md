# Assets for interview

Fake data samples for Data Engineering Case Study interview.

## Data sources:
1. csv files:
    * `marketing_campaigns.csv` with campaign dates, budgets, and channels
    * `user_discounts.csv` with selected user_id discount data